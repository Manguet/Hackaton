{% extends 'base.html.twig' %}

{% block title %}Multimedia{% endblock %}

{% block body %}
    <h1>Multimedia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ multimedia.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ multimedia.name }}</td>
            </tr>
            <tr>
                <th>URL</th>
                <td>{{ multimedia.URL }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('multimedia_index') }}">back to list</a>

    <a href="{{ path('multimedia_edit', {'id': multimedia.id}) }}">edit</a>

    {{ include('multimedia/_delete_form.html.twig') }}
{% endblock %}
