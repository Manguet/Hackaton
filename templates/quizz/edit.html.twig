{% extends 'base.html.twig' %}

{% block title %}Editer Quizz{% endblock %}

{% block body %}
    <div class="mt-5 mb-5 mr-5 ml-5">
        <h1>Editer Quizz</h1>

        {{ include('quizz/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('quizz_index') }}" class="btn btn-custom">Retour</a>

        {{ include('quizz/_delete_form.html.twig') }}
    </div>
{% endblock %}
